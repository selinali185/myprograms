# sum of perfect numbers under 10,000

num = int(input("Enter your number: "))

total_sum = 0
for num in range(1, 10000):
    factor_sum = 0 # factor_sum resets to 0 at every function
    for num in range(1, num + 1):
        if num % divider == 0:
            factor_sum += divider
    # end of for loop
    if factor_sum == num:
        # we have a perfect number
        total_sum += num

print(f" the total sum of all perfect numbers under 10,000 is {total_sum}. ")   

        