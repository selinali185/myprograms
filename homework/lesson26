# intro to functions

# determine # of factors from 1 to N

# before functions 
upper_limit = int(input("enter N: "))

for num in range(1, upper_limit + 1):
    facotr_size = factor_count(num)
    print(f"{num} has {facotr_size} factors")

#using functions

def factor_count(number):
    counter = 0 
    for divider in range(1, number + 1):
        if number % divider == 0:
            couter += 1
    return counter

    