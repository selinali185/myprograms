# cleaning a string

# removing non-alphpa characters from string

def string_cleaner(text):
    '''
    '''

    result = ""
    for character in text:
        if character.isalpha():
            result += character.lower()
    return result 

# example
value = string_cleaner("HELLO, WORLD!")
print(f"{value}")